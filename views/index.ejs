<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <style>
        body {
            background: linear-gradient(60deg, #f79533, #f37055, #ef4e7b, #a166ab, #5073b8, #1098ad, #07b39b, #6fba82);
            font-family: Arial, sans-serif;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }

        h1 {
            text-align: center;
            color: #fff;
            font-size: 3rem;
            margin-bottom: 30px;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 30px;
        }

        label {
            color: #fff;
            font-size: 1.5rem;
            margin-bottom: 10px;
        }

        input {
            width: 100%;
            max-width: 500px;
            height: 40px;
            border-radius: 20px;
            border: none;
            padding: 0 20px;
            font-size: 1.2rem;
            margin-bottom: 20px;
        }

        button {
            background-color: #fff;
            color: #000;
            border-radius: 20px;
            border: none;
            padding: 10px 20px;
            font-size: 1.2rem;
            cursor: pointer;
        }

        button:hover {
            background-color: #000;
            color: #fff;
        }

        table {
            border-collapse: collapse;
            width: 100%;
            margin-bottom: 30px;
        }

        th,
        td {
            text-align: left;
            padding: 10px;
        }

        th {
            background-color: #000;
            color: #fff;
            font-size: 1.2rem;
        }

        td {
            background-color: #fff;
            font-size: 1.1rem;
            border-bottom: 1px solid #ddd;
        }
    </style>
</head>
<body>
    <h1>URL Shortener</h1>
    <div class="container">
        <form action="/SHORTURL" method="POST" class="my-4 form-inline d-flex justify-content-between">
            <label for="fullUrl" class="visually-hidden"></label>
            <input required type="url" placeholder="URL" id="fullUrl" name="FULLURL" class="form-control m-2 col flex-grow-1">
            <button class="btn btn-success m-2" type="submit">Shrink</button>
        </form>
        <table class="table table-striped table-responsive">
            <thead>
                <tr>
                    <th>Full URL</th>
                    <th>Short URL</th>
                    <th>Clicks</th>
                </tr>
            </thead>
            <tbody>
                <% ALL_URLS.forEach((url) => { %>
                    <tr>
                        <td><a href="<%=url.fullurl_s%>"><%=url.fullurl_s%></a></td>
                        <td><a href="<%=url.shorturl_s%>"><%=url.shorturl_s%></a></td>
                        <td><%=url.clicks_s%></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>
